- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.tmux.conf:
      force: true
    ~/.zshrc:
      force: true
    ~/.vimrc:
      force: true
    ~/.gitconfig:
      force: true
    ~/.gitignore:
      force: true
    ~/.zshenv:
